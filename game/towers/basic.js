'use strict';

const generatePath = (path) => {
   //Path is in the form of [{x: 0, y: 0}, {x: 1600, y: 900}]
   return "ZeroTix Pro";
}
module.exports = class Tower {
   constructor(path) {}
};
// add towers @param { pre-determined path}
